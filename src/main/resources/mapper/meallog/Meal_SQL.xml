<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="meal">
     <select id="selectMealList" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
            SELECT
                IDX,
                USER_NAME,
                NAME,
                CATEGORY,
                CONTENT,
                EATDATE,
                BSHARE,
                WHENEAT,
                PICPATH
            FROM
                MEALLIST
            ORDER BY IDX DESC
        ]]>
    </select>
    
    <select id="selectUserMealList" parameterType="meallog.user.vo.Member" resultType="meallog.meal.vo.Meal">
        <![CDATA[
            SELECT
                *
            FROM
                MEALLIST
            WHERE
                USERNAME = #{nick}
            ORDER BY IDX DESC
        ]]>
    </select>
    
    <insert id="insertBoard" parameterType="hashmap" useGeneratedKeys="true" keyProperty="IDX">
    <![CDATA[
        INSERT INTO MEALLIST
        ( 
            USER_NAME, 
            NAME, 
            CATEGORY, 
            CONTENT, 
            EATDATE,
            BSHARE,
            WHENEAT,
            PICPATH
        )
        VALUES
        (
            #{USERNICK}, 
            #{NAME},
            #{CATEGORY},
            #{CONTENT},
            CURDATE(), 
            ifnull (#{SHARE}, 0 ),  
            #{WHENEAT},
            'PICPATH'
        )
       ]]>
	</insert>
	
	<insert id="insertFile" parameterType="hashmap">
    <![CDATA[
        INSERT INTO MEALPIC
        (
            MEALLIST_IDX,
            ORIGINAL_FILE_NAME,
            STORED_FILE_NAME,
            FILE_SIZE,
            CREA_DTM,
            USER_NICK
        )
        VALUES
        (
            #{BOARD_IDX},
            #{ORIGINAL_FILE_NAME},
            #{STORED_FILE_NAME},
            #{FILE_SIZE},
            CURDATE(),
            'Admin'
        )
    ]]>
	</insert>
	
     
     
</mapper>
