<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="meal">
     <select id="selectFileList" parameterType="hashmap" resultType="hashmap">
    <![CDATA[
        SELECT
            IDX,
            ORIGINAL_FILE_NAME,
            ROUND(FILE_SIZE/1024,1) AS FILE_SIZE
        FROM
            MEALPIC
        WHERE
            MEALLIST_IDX = #{IDX}
            AND DEL_GB = 'N'
    ]]>
	</select>
    
    <select id="selectUserMealList" parameterType="meallog.user.vo.Member" resultType="meallog.meal.vo.Meal">
        <![CDATA[
            SELECT
                *
            FROM
                MEALLIST
            WHERE
                USERNAME = #{nick}
            ORDER BY IDX DESC
        ]]>
    </select>
    
    <insert id="insertBoard" parameterType="hashmap" useGeneratedKeys="true" keyProperty="IDX">
    <![CDATA[
        INSERT INTO MEALLIST
        ( 
            USERNAME, 
            NAME, 
            CATEGORY, 
            CONTENT, 
            EATDATE,
            SHARE,
            WHENEAT,
            PICPATH
        )
        VALUES
        (
            #{USERNICK}, 
            #{NAME},
            #{CATEGORY},
            #{CONTENT},
            CURDATE(), 
            ifnull (#{SHARE}, 0 ),  
            #{WHENEAT},
            'PICPATH'
        )
       ]]>
	</insert>
	 <select id="updateFilePath" parameterType="hashmap" resultType="hashmap">
    <![CDATA[
        UPDATE
        	MEALLIST
        SET
        	PICPATH = #{PICPATH}
        WHERE
        	IDX = #{IDX}
    ]]>
	</select>
	<insert id="insertFile" parameterType="hashmap">
    <![CDATA[
        INSERT INTO MEALPIC
        (
            MEALLIST_IDX,
            ORIGINAL_FILE_NAME,
            STORED_FILE_NAME,
            FILE_SIZE,
            CREA_DTM,
            USER_NICK
        )
        VALUES
        (
            #{BOARD_IDX},
            #{ORIGINAL_FILE_NAME},
            #{STORED_FILE_NAME},
            #{FILE_SIZE},
            CURDATE(),
            'Admin'
        )
    ]]>
	</insert>
	
     
     
</mapper>
